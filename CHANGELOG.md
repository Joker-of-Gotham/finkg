# Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.0] — 2026-08-13

针对实际使用中的三个严重问题：根目录堆文件、边名写成句子、规模/跳数硬门禁。

- **草稿进会话 `drafts/`。** `fg template` 默认写入 `financial-graph-sessions/<会话>/drafts/`；相对路径不再落到工作区根目录。`fg harvest mine --done` 自动读取 `drafts/entities.json` 与 `drafts/fact*.json`。`fg brief` 会点名根目录 stray 文件。
- **关系类型改为 2–8 字中文短语。** 对象、品类、限定写进 `attrs`。整句（如「向客户长协供应电池级碳酸锂」）与空泛词同样判 error。
- **去掉节点数/边数/跳数门槛。** `quality` 的 `ok=false` 只表示证据缺陷或边名不可读。规模与扇区缺口变成 `level: guide`，并给出下一条该问的检索。`fg neo4j load` 只拦引文找不到、端点悬空；不再因为「不够 120 个节点」拒装。

## [1.0.0] — 2026-08-13

首个公开版本。这是一次彻底重写：上一代实现（八道阻断 Gate、HMAC attestation、一次性 resume token、
工件哈希链、约 9,600 行运行时、26 份参考文档中 21 份已过时）被整体废弃，没有任何代码或文档延续。

### 零部署信息

仓库里不含任何主机、账号、密码、内部数仓库表名或内部工具专有名称。

- 地址与凭据**没有默认值**，必须由使用者配置到工作区 `financial_graph.local.json`
  或 `FG_*` 环境变量；未配置时命令报错并直接列出要填什么、填到哪里。
- `fg setup` 一条命令生成本地配置，并检查它是否已被 `.gitignore` 覆盖。
- 「这套部署有哪些数据表与检索工具」属于使用者的内部信息，记在工作区
  `finkg.environment.md`（`fg env --init` 生成），不进仓库；skill 只教如何探索与记录。
- `fg doctor` 默认输出不含主机、账号、密码，也不列其他项目的数据库名，可直接贴进 issue；
  本机排错用 `--verbose`。
- `TestNoDeploymentSpecifics` 做全仓库正则扫描（内网 IP、真实域名、凭据字面量、
  内部数仓 schema），CI 单独设一个 job 守着。

### 核心能力

- **LazySearch 全量落盘与切层。** HTTP `/api/query` 与 MCP 双通道，返回按结论 / 原始数据 /
  查询计划 / 服务端提示四层切开，可分层按需取用而不必整块进上下文。实测一次返回中结论
  仅占信息量 4%，其余在 `history` 的原始工具返回里。
- **信息利用率逐单元格核账。** 清点每次收割的数据单元格，逐格判定「已进图 / 已写明不用 /
  仍悬空」，并给出未用数据的具体样本。要么用，要么交代，静默丢弃不通过。
- **节点即详表。** 属性用中文点分层级，第一段自动成为维度组；每个属性带单位、币种、期间、
  合并口径、来源事实与可核验引文。引文必须能在落盘原文里逐字找到，否则拒绝入库。
- **边可分析。** Neo4j 关系类型直接是中文金融动作，强制携带一句话传导机制与量化属性；
  「相关 / 影响 / 关联」一类空泛关系判为不可分析。
- **纵深有实质性判据。** 只计业务实体跳；长路径需同时满足跨 ≥3 语义层、关系类型数
  ≥⌈跳数/2⌉、逐跳有证据与机制、连续推断 ≤2 跳，并要求边不重叠的独立见证。
- **Neo4j 单事务装载。** 整库替换 + 同事务内读回核对，核对不上整体回滚，库内容不变。
- **多阶段人在回路，零仪式。** 结构化提问 + 明文 JSONL 台账。无 Gate、无签名、无 HMAC、
  无一次性令牌、无工件哈希链、无密钥环境变量。
- **质量只判内容。** 不校验 JSON 键位、不算工件哈希、不把节点数量当目标。

### 工程

- 7 个 Python 模块共约 2,950 行，**仅标准库**，Python 3.10+。
- 跨宿主安装器：Claude Code、Cursor、Codex、OpenCode、Gemini CLI、Copilot、Windsurf，
  以及 Agent Skills 通用路径 `.agents/skills/`。符号链接优先，Windows 自动回退复制。
- 支持 `npx skills add`、`npx @skillhub/cli install`、`git clone` + `install.py`、
  Claude Code 插件市场四种安装途径。
- Windows 专项：BOM 与 UTF-16 自动识别；针对 PowerShell 会按控制台宽度折断长字符串的问题，
  提供全局 `--out-file`（子命令前后均可放置）。
- 离线单测不依赖 Neo4j 与 LazySearch。

### 相对上一代的取舍

| 上一代 | 现在 | 原因 |
| --- | --- | --- |
| G1–G8 八道阻断 Gate | 结构化提问 + 明文台账 | 门禁保护的是流程合规，不是判断质量 |
| HMAC attestation + resume token | 无 | 无法抵抗恶意 Agent，只增加摩擦 |
| 工件哈希链与快照摘要 | 无 | 校验的是形式而非内容 |
| 模板字段目录完整度 | 已知且有证据的属性数 | 填满空字段让图更差而非更好 |
| 只用检索结论 | 逐单元格核账原始返回 | 结论只占返回信息量的 4% |
